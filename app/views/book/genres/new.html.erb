<div>
  <h1>New genre</h1>
  <table class="showtable">
    <% form_for @genre, :url => book_genres_path do |f| %>
      <%= render(:partial => 'form', :locals => { :f => f, :genre => @genre }) %>
      <tr>
        <td colspan="2"><%= f.submit "Create" %></td>
      </tr>
    <% end %>
    <tr class="button_row">
      <td colspan="2"><%= link_to "Back", book_genres_path %></td>
    </tr>
  </table>
</div>