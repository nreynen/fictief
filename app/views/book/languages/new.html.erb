<div>
  <h1>New language</h1>
  <table class="showtable">
    <% form_for @language, :url => book_languages_path do |f| %>
      <%= render(:partial => 'form', :locals => { :f => f, :language => @language }) %>
      <tr>
        <td colspan="2"><%= f.submit "Create" %></td>
      </tr>
    <% end %>
    <tr class="button_row">
      <td colspan="2"><%= link_to "Back", book_languages_path %></td>
    </tr>
  </table>
</div>