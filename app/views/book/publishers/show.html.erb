<div>
  <h1><%= @publisher[:name] %></h1>
  <table class="showtable">
    <tr>
      <td>Name</td>
      <td><%= @publisher[:name] %></td>
    </tr>
    <tr>
      <td>Place</td>
      <td><%= @publisher[:place] %></td>
    </tr>
    <tr class="button_row">
      <td colspan="2"><%= link_to "Edit", edit_book_publisher_path(@publisher) %> | 
        <%= link_to "Destroy", book_publisher_path(@publisher), :confirm => "Are you sure?", :method => :delete %> | 
        <%= link_to "Back", book_publishers_path %></td>
    </tr>
  </table>
</div>