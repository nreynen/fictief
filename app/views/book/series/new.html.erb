<div>
  <h1>New serie</h1>
  <table class="showtable">
    <% form_for @serie, :url => book_series_index_path do |f| %>
      <%= render(:partial => 'form', :locals => { :f => f, :serie => @serie }) %>
      <tr>
        <td colspan="2"><%= f.submit "Create" %></td>
      </tr>
    <% end %>
    <tr class="button_row">
      <td colspan="2"><%= link_to "Back", book_series_index_path %></td>
    </tr>
  </table>
</div>