<% content_for :head do %>
  <%= javascript_include_tag "BreadApp/order.js" %>
<% end %>

<div>
  <h1>Edit order</h1>
  <table class="showtable">
    <% form_for @order, :url => bread_weekly_order_path(@order) do |f| %>
      <%= render(:partial => 'form', :locals => { :f => f, :order => @order }) %>
      <tr>
        <td colspan="2"><%= f.submit "Update", :confirm => put("order_alert_update") %><span style="float: right;" id="overview_hover">Hover for overview</span></td>
      </tr>
    <% end %>
    <tr class="button_row">
      <td colspan="2"><%= back_link :url => bread_weekly_orders_path %></td>
    </tr>
  </table>
</div>