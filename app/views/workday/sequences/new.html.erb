<div>
  <h1>New sequence</h1>
  <table class="showtable">
    <% form_for @sequence, :url => workday_sequences_path do |f| %>
      <%= render(:partial => 'form', :locals => { :f => f, :sequence => @sequence }) %>
      <tr>
        <td colspan="2"><%= f.submit "Create" %></td>
      </tr>
    <% end %>
    <tr class="button_row">
      <td colspan="2"><%= link_to "Back", workday_sequences_path %></td>
    </tr>
  </table>
</div>