<h1>New User</h1>

<%= form_for @usr, :url => users_path do |f| %>
  <%= render(:partial => 'form', :locals => { :f => f, :user => @usr }) %>
<% end %>

<div class="submit_row">
  <%= link_to_function "Create", "$('form').last().submit();" %>
  <%= back_link :url => users_path %>
</div>